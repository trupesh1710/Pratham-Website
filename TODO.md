# TODO — SEO improvements (SPA best-effort)

- [ ] Add JSON-LD structured data + OpenGraph/Twitter refinements in `index.html` (Person/Organization schema).
- [ ] Add top-of-page keyword-rich copy for “Pratham in UK” in `Hero.tsx` (real text, not just typed animation).
- [ ] Ensure heading structure (one H1 in Hero; add/verify H2/H3 usage in sections).
- [ ] Add a canonical URL and additional meta tags in `index.html` (optionally theme/color consistency).
- [ ] Build & run `npm run build` to ensure nothing breaks.
- [ ] Recommend checking in Google Search Console + Rich Results Test for schema.

